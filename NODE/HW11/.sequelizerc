const path = require('path');

module.exports = {
  'config': path.join(process.cwd(), 'config', 'sequelize.config.js'),
  'models-path': path.join(process.cwd(), 'database', 'model'),
  'seeders-path': path.join(process.cwd(), 'database', 'seeder'),
  'migrations-path': path.join(process.cwd(), 'database', 'migration')
};
